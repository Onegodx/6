ф<Window x:Class="WpfApp5.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TextBlock Text="Введите количество элементов:" Margin="10" VerticalAlignment="Center" />
        <TextBox x:Name="tbN" Margin="10" Grid.Row="1" />
        <Button Content="Сортировать" Margin="10" Grid.Row="1" Click="Button_Click" HorizontalAlignment="Right" />
        <DataGrid x:Name="dg" Margin="10" Grid.Row="2" AutoGenerateColumns="True" />
    </Grid>
</Window>

